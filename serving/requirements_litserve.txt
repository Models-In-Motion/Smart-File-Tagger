# Requirements for LitServe deployment
# LitServe is built on FastAPI — adds ~20MB vs Ray Serve's ~500MB+

# LitServe (includes FastAPI + Uvicorn under the hood)
litserve>=0.2.0

# ML model
sentence-transformers==2.6.1
lightgbm==4.3.0
numpy==2.0.2
scikit-learn==1.5.2

# ONNX inference
onnxruntime==1.19.2
onnxscript==0.6.2
transformers>=4.36.0
