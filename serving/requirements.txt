# Core serving
fastapi==0.110.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9      # needed for FastAPI to accept file uploads

# Text extraction
pdfminer.six==20221105       # extracts text from PDFs
Pillow==10.2.0               # opens image files
pytesseract==0.3.10          # OCR — reads text from images (needs tesseract installed)

# ML model
# Force CPU-only torch wheels to avoid CUDA dependency bloat in Docker builds.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.5.1+cpu
sentence-transformers==2.3.1 # SBERT — converts text into embeddings
lightgbm==4.3.0              # the actual classifier
numpy==1.26.4                # must match pickle layout (1.26 → ModuleNotFoundError: numpy._core.numeric)
scikit-learn==1.5.2          # NumPy 2–compatible; label encoder + similarity

# Database
psycopg2-binary==2.9.9       # PostgreSQL connection

# Utilities
python-dotenv==1.0.1         # reads .env file for secrets
pydantic==2.6.4              # data validation (FastAPI uses this under the hood)
httpx==0.27.2
onnxruntime==1.19.2
onnxscript==0.6.2            # required by torch.onnx.export on PyTorch 2.x (Docker build runs export)
gunicorn==21.2.0

# Monitoring
prometheus-fastapi-instrumentator==6.1.0  # auto-instruments FastAPI with request metrics
prometheus-client==0.20.0                 # exposes custom counters and histograms

# mlflow<2.16 pins numpy<2, which conflicts with numpy==1.26.4 above (joblib bundle).
mlflow==2.16.2
